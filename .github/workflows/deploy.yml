name: Deploy Cadenza Studio to GitHub Pages

on:
  push:
    branches: ["main", "master"]
  workflow_dispatch:

# Permisos requeridos por GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Instalar dependencias del cliente
        run: npm --prefix client install

      - name: Construir aplicación Web (Offline Standalone)
        run: npm --prefix client run build

      - name: Configurar GitHub Pages
        uses: actions/configure-pages@v4

      - name: Subir artefacto de construcción
        uses: actions/upload-pages-artifact@v3
        with:
          path: './client/dist'

      - name: Desplegar en GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
